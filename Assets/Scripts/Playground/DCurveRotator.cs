using UnityEngine;
using System.Collections;

namespace Telescopes
{
    public class DCurveRotator : MonoBehaviour
    {
        public DiscreteCurve target;

        // Update is called once per frame
        void Update()
        {
            if (Input.GetKey("r"))
            {

            }
        }
    }

}